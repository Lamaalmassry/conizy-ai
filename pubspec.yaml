name: conizy_flutter
description: Conizy MVP mobile UI
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2
  path: ^1.9.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/onboarding1_bg.png
    - assets/onboarding2.png
    - assets/onboarding3.png
    - assets/logo.png
